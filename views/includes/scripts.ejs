<script>
	// const cookieHueValue = document.cookie
	// 	.split("; ")
	// 	.find((row) => row.startsWith("hue="))
	// 	?.split("=")[1]

	// const rootEl = document.querySelector(":root")
	// const rootElStyle = getComputedStyle(rootEl)
	// const cssColPrimary = rootElStyle.getPropertyValue("--col-primary")

	// function setHue(value) {
	// 	// const rootEl = document.querySelector(":root")
	// 	// const rootElStyle = getComputedStyle(rootEl)
	// 	// const cssColPrimary = rootElStyle.getPropertyValue("--col-primary")
	// 	rootEl.style.setProperty("--col-primary", `oklch(from ${cssColPrimary} l c ${value})`)
	// }

	// if (!!cookieHueValue) {
	// 	setHue(cookieHueValue)
	// }
</script>