<nav class="navbar">
  <ul>
    <% for (const link of links) { %>
    <li>
      <a 
				<% if (link.title !== title) { %>
					href="<%= link.url %>" 
				<% } %>
			>
				<%= link.title %>
			</a>
    </li>
    <% } %>
  </ul>
</nav>
<%- include("spacer", { size: "lg" }) %>
